import type React from 'react';

// Provider switching is intentionally disabled.
// Keeping this component as a no-op avoids touching unrelated layout code.
export default function ProviderSwitcher(): React.ReactNode {
  return null;
}
